import 'package:flutter/widgets.dart';

class MPCTransactionPage extends StatelessWidget {
  const MPCTransactionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
